name = "blog-monumental"
account_id = "f58f884e92d89c2345cb3a2df450a838"
compatibility_date = "2026-07-29"
pages_build_output_dir = "dist"

# Secrets/vars de runtime (setar via `wrangler pages secret put <NAME> --project-name blog-monumental`):
#   GA4_MEASUREMENT_ID = G-715ZBSK42X  (mesmo GA4 do site principal monumentalcontabilidade.com.br)
#   DASH_KEY           = <chave admin /admin>
# Build var (público): PUBLIC_META_PIXEL_ID = (vazio — cliente não tem pixel ainda)
# Lembrete: CF Pages só binda secret em NOVO deploy → após secret put, rebuild + redeploy.
